[Slice]
# Companion to the sshd reservation: getting a connection accepted is no use if
# the resulting session cannot allocate. 512M covers the shell plus ps, py-spy
# and the tools needed to diagnose a struggling host.
MemoryMin=512M
